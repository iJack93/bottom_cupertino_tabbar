name: bottom_cupertino_tabbar
description: "A custom enhanced cupertino tabbar ready to use"
version: 1.0.0
homepage: https://github.com/iJack93/bottom_cupertino_tabbar

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - test/assets/bottom/
